This project shows off the use of pthreads and multi threading allowing each thread to compute each matrix cell in parallel with the .txt files shown. The main thread will create multiple threads. Each additional thread will solve one cell of the matrix
multiplication problem.
